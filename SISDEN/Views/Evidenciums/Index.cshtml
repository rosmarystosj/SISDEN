@model IEnumerable<SISDEN.Models.Evidencium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Evurl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EvIdtipoevid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EvIddenuncia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Evurl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EvIdtipoevid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EvIddenuncia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idevidencia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idevidencia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idevidencia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
